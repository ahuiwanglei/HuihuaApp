<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- 填充 -->
    <solid android:color="@color/white" />
    <stroke
        android:width="1dp"
        android:color="@color/default_bar_bg" />
    <size android:height="1.0dp"/>
    <!-- 圆角 -->
    <corners android:radius="5dp" />
</shape>